# IC-Expert Agent 项目掌握度自测表

> 使用方式：不要查答案，先独立填写。每题尽量写到“模块名 + 文件位置 + 为什么这样设计”。  
> 评分建议：0 = 不会；1 = 有印象但讲不顺；2 = 能讲清主线；3 = 能结合代码、风险和改进讲清楚。

## 0. 基本信息

- 填写日期：2026/05/10
- 当前自评总分：
- 最卡的 3 个点：
  1.项目不是自己做的，很多细节不清楚，都是vibing coding，感觉自己像个冒险者，随时可能被问到“这个是怎么实现的？”然后蒙圈。
  2.感觉自己啥也不会，找不到好的实习
  3.

## 1. 一句话讲项目

### 1.1 30 秒版本

请用 3 到 5 句话讲清楚这个项目是什么、解决什么问题、用了哪些核心技术。

我的回答：

```text
这个项目是一个ic知识问答系统，可以根据用户提的问题，结合内置的工具（知识搜素，verilog代码检查，时序分析）和知识库，选择调用哪个工具，最后把工具的结果喂给大模型，大模型基于工具的结果进行回答。解决了用户在ic领域提问时候，模型回答不准确，出现幻觉的问题。技术包括：fastapi，langgraph,llamaindex,chroma,huggingface embedding+crossencoder,reranker,pydantic,redis等
```

自评分：

改进点：

### 1.2 面试开场版本

请按“背景 - 目标 - 方案 - 结果”讲一遍，不要堆技术名词。

我的回答：

```text

```

自评分：

改进点：

## 2. 用户请求链路

### 2.1 请求从哪里进来？

请写出用户请求进入服务的入口文件、路由注册位置、核心接口路径。

我的回答：

```text

```

需要能提到：

- FastAPI app 在哪里创建
- `/api/v1/chat` 和 `/api/v1/chat/stream` 在哪里定义
- 请求体和响应体用哪些 schema

自评分：

### 2.2 `/chat` 完整链路

请从用户发起请求开始，一直写到最终返回 `ChatResponse`。要求写出关键模块顺序。

我的回答：

```text
用户请求 ->

```

自评分：

### 2.3 `/chat/stream` 和 `/chat` 有什么区别？

请说明两者返回方式、适用场景、SSE 事件类型。

我的回答：

```text

```

自评分：

## 3. Agent 主流程

### 3.1 LangGraphICAgent 做了什么？

请写出 LangGraph 里有哪些节点，每个节点负责什么。

我的回答：

```text

```

需要能提到：

- `pre_tool_router`
- `tool_executor`
- `answer_generator`
- `clarify`
- `START` / `END`

自评分：

### 3.2 pre_tool_router 怎么决定调用哪些工具？

请说明它怎么判断 IC 领域问题、Verilog 问题、时序问题、短问题、越界问题。

我的回答：

```text

```

自评分：

### 3.3 answer_generator 为什么不是直接问大模型？

请说明工具结果、严格拒答、system prompt、引用重写之间的关系。

我的回答：

```text

```

自评分：

### 3.4 这个 Agent 和普通聊天机器人有什么区别？

我的回答：

```text

```

自评分：

## 4. ToolRegistry 和工具系统

### 4.1 ToolRegistry 是什么？

请说明它在项目里解决了什么工程问题。

我的回答：

```text

```

自评分：

### 4.2 当前有哪些工具？

请写出工具名、用途、输入、输出。

| 工具名 | 用途 | 输入 | 输出 | 什么时候会被调用 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |

自评分：

### 4.3 如果新增一个“功耗分析工具”，应该改哪些地方？

请写出你的设计步骤，不需要写代码。

我的回答：

```text

```

自评分：

## 5. RAG 部分

### 5.1 RAG 建库流程是什么？

请按 ETL 角度说明：Extract、Transform、Load 分别做什么。

我的回答：

```text

```

需要能提到：

- PDF/TXT 解析
- chunk 切分
- metadata
- embedding
- Chroma 持久化
- 数据源变化后的 rebuild

自评分：

### 5.2 查询时 RAG 怎么跑？

请写出从用户 query 到返回 sources 的完整流程。

我的回答：

```text

```

需要能提到：

- query expansion
- vector retrieval
- candidate top-k
- reranker
- source/page/chunk_id/score
- citation rewrite

自评分：

### 5.3 为什么要自定义 IC chunker？

请说明普通切分可能带来的问题，以及这个项目想保留哪些结构。

我的回答：

```text

```

自评分：

### 5.4 为什么需要 reranker？

请说明向量召回和 CrossEncoder 重排的区别。

我的回答：

```text

```

自评分：

### 5.5 如果用户问了知识库没有的问题，系统应该怎么做？

请结合严格拒答、工具结果、引用重写回答。

我的回答：

```text

```

自评分：

## 6. ModelRouter

### 6.1 ModelRouter 是什么？

请说明它和 `pre_tool_router` 的区别。

我的回答：

```text

```

自评分：

### 6.2 它支持哪些能力？

请说明模型选择、优先级、权重、失败降级、熔断。

我的回答：

```text

```

自评分：

### 6.3 当前项目真的用了多模型吗？

请说明“架构支持”和“当前配置”的区别。

我的回答：

```text

```

自评分：

## 7. Trace 和排错

### 7.1 trace_id 做到了哪一步？

请说明 trace_id 什么时候生成、返回到哪里、目前缺什么能力。

我的回答：

```text

```

自评分：

### 7.2 出错应该去哪查？

请分别说明接口错误、Agent 路由错误、工具错误、RAG 检索错误、模型调用错误应该看哪里。

我的回答：

```text

```

自评分：

### 7.3 如果用户说“答案不准”，你怎么排查？

请给出排查顺序。

我的回答：

```text
1.
2.
3.
4.
5.
```

自评分：

## 8. 评估体系

### 8.1 RAGAS 是什么？

请说明它评估 RAG 的哪些方面。

我的回答：

```text

```

自评分：

### 8.2 项目里用了哪些指标？

请写出指标名和含义。

| 指标 | 含义 | 高说明什么 | 低可能是什么问题 |
| --- | --- | --- | --- |
| faithfulness |  |  |  |
| answer_relevancy |  |  |  |
| context_recall |  |  |  |
| context_precision |  |  |  |
| citation_correctness |  |  |  |
| refusal_correctness |  |  |  |
| tool_routing_accuracy |  |  |  |

自评分：

### 8.3 如果 context_recall 低，你会怎么优化？

我的回答：

```text

```

自评分：

### 8.4 如果 faithfulness 低，你会怎么优化？

我的回答：

```text

```

自评分：

## 9. 工程优化题

### 9.1 为什么要把 ModelRouter、ToolRegistry、LangGraphICAgent 做成 singleton？

请说明当前问题、优化方式、注意事项。

我的回答：

```text

```

自评分：

### 9.2 如果要补“按照 trace_id 查询问题”的模块，你怎么做？

请写出 API、数据库表、写入时机、查询返回内容。

我的回答：

```text

```

自评分：

### 9.3 当前项目有哪些不足？

请至少写 5 条，并说明怎么改。

| 不足 | 影响 | 改进方案 |
| --- | --- | --- |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |

自评分：

## 10. 简历和 STAR

### 10.1 简历项目描述

请写一段 4 到 6 行的简历项目经历，要求包含技术栈、职责、结果指标。

我的回答：

```text

```

自评分：

### 10.2 STAR 版本

S - Situation：

```text

```

T - Task：

```text

```

A - Action：

```text

```

R - Result：

```text

```

自评分：

### 10.3 面试官追问：这个项目最难的点是什么？

请不要回答“技术很多”。要回答一个具体技术难点。

我的回答：

```text

```

自评分：

### 10.4 面试官追问：你负责哪部分？

请按真实可解释的方式回答，不要把所有开源组件都说成自己从零实现。

我的回答：

```text

```

自评分：

## 11. 代码定位题

请填写每个功能对应的关键文件。

| 功能 | 关键文件 | 我为什么认为是这里 |
| --- | --- | --- |
| FastAPI app 创建 |  |  |
| `/chat` 接口 |  |  |
| `/chat/stream` 接口 |  |  |
| 请求/响应 schema |  |  |
| LangGraph Agent |  |  |
| 工具注册 |  |  |
| IC RAG 工具 |  |  |
| Retriever |  |  |
| Reranker |  |  |
| Citation rewrite |  |  |
| ModelRouter |  |  |
| CircuitBreaker |  |  |
| Trace |  |  |
| Document upload |  |  |
| ETL parser |  |  |
| Chunker |  |  |
| RAGAS 评估 |  |  |

自评分：

## 12. 模拟面试快问快答

每题限制 60 秒内说清楚。

### Q1：你的 RAG 为什么不直接把所有文档塞给大模型？

我的回答：

```text

```

### Q2：为什么向量召回之后还要重排？

我的回答：

```text

```

### Q3：怎么避免模型编造引用？

我的回答：

```text

```

### Q4：LangGraph 在这里解决了什么问题？

我的回答：

```text

```

### Q5：SSE 为什么不用 WebSocket？

我的回答：

```text

```

### Q6：如果模型服务不可用，系统怎么处理？

我的回答：

```text

```

### Q7：如果知识库文档更新，向量库怎么同步？

我的回答：

```text

```

### Q8：你这个项目怎么证明效果？

我的回答：

```text

```

### Q9：这个项目上线前还缺什么？

我的回答：

```text

```

### Q10：你最想重构哪里？

我的回答：

```text

```

## 13. 总结

### 我现在最熟的 3 个点

1.
2.
3.

### 我现在最不熟的 3 个点

1.
2.
3.

### 下一轮要重点补的内容

1.
2.
3.

### 最终自评

- 请求链路：
- Agent：
- RAG：
- 工程排错：
- 评估：
- 简历表达：
- 综合掌握度：
